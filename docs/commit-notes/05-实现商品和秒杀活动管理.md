# 第 05 次提交：实现商品和秒杀活动管理

## 本次目标

让系统第一次具备“真实业务数据入口”。

这一提交要完成：

1. 商品创建
2. 商品列表查询
3. 秒杀活动创建
4. 秒杀活动列表查询
5. 活动时间校验和库存初始化

## 我这次做了什么

1. 增加了商品模型、仓储、服务和接口
2. 增加了秒杀活动模型、库存模型、仓储、服务和接口
3. 创建秒杀活动时，自动初始化库存记录
4. 增加活动时间窗口校验
5. 新增接口：
   - `POST /api/v1/products`
   - `GET /api/v1/products`
   - `POST /api/v1/activities`
   - `GET /api/v1/activities`

## 本次如何测试

### 1. 自动化测试

```bash
go test ./...
```

### 2. 手动验证

1. 登录获取 token
2. 用 token 创建商品
3. 用 token 创建秒杀活动
4. 查询商品列表
5. 查询活动列表

## 本次实际验证结果

- `go test ./...`：通过
- `POST /api/v1/products`：返回 HTTP 200，商品创建成功
- `POST /api/v1/activities`：返回 HTTP 200，活动创建成功并初始化库存
- `GET /api/v1/products`：返回 HTTP 200，列表中能看到新建商品
- `GET /api/v1/activities`：返回 HTTP 200，列表中能看到新建活动，库存字段正确返回
