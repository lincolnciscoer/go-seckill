# 第 06 次提交：实现活动预热与缓存化查询

## 本次目标

把活动查询从“直接查库”升级成“缓存优先、数据库兜底”的模式。

这一提交要完成：

1. 活动详情缓存
2. 活动列表缓存
3. 活动预热接口
4. 缓存未命中时自动回源数据库并回填缓存

## 我这次做了什么

1. 增加了活动缓存封装
2. 增加了活动详情查询接口
3. 增加了活动预热接口
4. 让活动列表和活动详情都先查 Redis，再回源 MySQL
5. 预热时同时写入：
   - 活动详情 key
   - 库存 key
   - 活动列表 key

## 本次如何测试

### 1. 自动化测试

```bash
go test ./...
```

### 2. 手动验证

1. 创建商品和活动
2. 调用预热接口
3. 检查 Redis 里是否生成 `seckill:*` key
4. 调用活动列表和详情接口
5. 删除缓存 key 后再次请求，确认 key 被重新回填

## 本次实际验证结果

- `go test ./...`：通过
- `POST /api/v1/activities/{id}/preheat`：返回 HTTP 200
- 预热完成后，Redis 中成功生成：
  - `seckill:activity:list`
  - `seckill:activity:{id}`
  - `seckill:stock:{id}`
- 删除列表和详情 key 后，再次访问：
  - `GET /api/v1/activities`
  - `GET /api/v1/activities/{id}`
  两个接口都返回 HTTP 200，并且缓存 key 被重新回填
