# go-seckill 架构图与时序图版

## 1. 系统总览

```mermaid
flowchart LR
    Client["Client / LoadTest"] --> API["Gin API"]
    API --> Auth["JWT Auth"]
    API --> Guard["Rate Limit + Duplicate Submit Guard"]
    Guard --> Redis["Redis + Lua"]
    Redis --> MQ["RocketMQ"]
    MQ --> Consumer["Order Consumer"]
    Consumer --> MySQL["MySQL"]
    API --> MySQL
    API --> Metrics["Prometheus Metrics"]
    API --> Trace["OpenTelemetry Trace"]
    Consumer --> Metrics
    Consumer --> Trace
    Metrics --> Prom["Prometheus"]
    Prom --> Grafana["Grafana"]
    Trace --> Jaeger["Jaeger"]
```

## 2. 秒杀主链路

```mermaid
sequenceDiagram
    participant U as User
    participant A as API
    participant R as Redis Lua
    participant M as RocketMQ
    participant C as Consumer
    participant D as MySQL

    U->>A: POST /seckill/activities/{id}/attempt
    A->>R: Lua原子校验 + 预扣减库存
    alt 准入失败
        R-->>A: sold_out / repeat / not_started
        A-->>U: 409 / 400
    else 准入成功
        R-->>A: allow
        A->>M: 发送秒杀订单消息
        M-->>A: message_id
        A-->>U: 200 queued + order_no
        M->>C: 投递消息
        C->>D: 幂等校验 + 扣库存 + 创建订单
        D-->>C: 提交成功
        C-->>M: Ack
    end
```

## 3. 订单查询链路

```mermaid
sequenceDiagram
    participant U as User
    participant A as API
    participant R as Redis Order Status
    participant D as MySQL

    U->>A: GET /orders/{orderNo}
    A->>D: 查询订单
    alt 订单已落库
        D-->>A: order created
        A-->>U: 200 OK
    else 未落库
        A->>R: 查询订单状态缓存
        alt 状态为 queued
            R-->>A: queued
            A-->>U: 202 ORDER_PROCESSING
        else 无状态
            A-->>U: 404 ORDER_NOT_FOUND
        end
    end
```

## 4. 可观测链路

```mermaid
flowchart TD
    API["API /metrics"] --> Prom["Prometheus"]
    Consumer["Consumer /metrics"] --> Prom
    Prom --> Grafana["Grafana Dashboard"]

    APITrace["API Trace"] --> OTLP["OTLP Exporter"]
    ConsumerTrace["Consumer Trace"] --> OTLP
    OTLP --> Jaeger["Jaeger UI"]

    API["API /debug/pprof/*"] --> Pprof["pprof"]
    Consumer["Consumer /debug/pprof/*"] --> Pprof
```

## 5. 面试时如何配合图来讲

### 总图怎么讲

先讲系统分层：

1. Gin 对外提供 HTTP API
2. Redis + Lua 负责秒杀高并发准入
3. RocketMQ 负责削峰填谷
4. Consumer 负责异步落库
5. MySQL 负责最终一致性
6. Prometheus / Grafana / Jaeger / pprof 负责可观测

### 主链路图怎么讲

先说同步版做过什么，再说为什么升级成异步版：

- 同步版容易理解，但高并发下数据库压力大
- 所以后来把准入前置到 Redis Lua
- 再把下单写库改成 RocketMQ 异步消费
- 这样 API 返回更快，数据库压力更可控

### 查询链路图怎么讲

这里重点突出异步系统的用户体验设计：

- API 先返回 queued
- 订单未落库时不直接返回 404
- 通过 Redis 状态缓存返回 `202 ORDER_PROCESSING`

这能说明你不仅关注后端吞吐，也关注真实用户体验。
