# 第 12 次提交：补压测方案、性能报告和项目展示材料

## 本次目标

让项目从“功能和工程都完成”走到“可以拿去展示”。

这一提交要完成：

1. 仓库内置压测工具
2. 一次可复现的压测结果
3. README 中的项目亮点、架构和展示信息补齐

## 我这次做了什么

1. 增加了 `cmd/loadtest`
   - 自动注册用户
   - 自动创建商品和活动
   - 自动预热缓存
   - 并发发起秒杀请求
   - 汇总 HTTP 状态、业务码和延迟统计
2. 增加了性能报告文档
3. 补充 README 中的项目展示内容

## 本次如何测试

### 1. 编译压测工具

```bash
go build ./cmd/loadtest
```

### 2. 运行压测

```bash
go run ./cmd/loadtest --base-url http://127.0.0.1:18092 --users 40 --concurrency 20 --stock 20
```

## 本次实际验证结果

- 已成功编译并运行 `cmd/loadtest`
- 样例一：
  - `users=20`
  - `concurrency=10`
  - `stock=20`
  - 结果为 `20` 个 `OK`
  - 平均延迟 `25.35 ms`
  - P95 `38 ms`
- 样例二：
  - `users=40`
  - `concurrency=20`
  - `stock=20`
  - 结果为 `20` 个 `OK`、`20` 个 `RATE_LIMITED`
  - 平均延迟 `34.8 ms`
  - P95 `79 ms`
- 性能报告已同步写入 `docs/performance-report.md`
